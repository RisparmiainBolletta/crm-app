services:
  - type: web
    name: crm-app
    env: python
    buildCommand: ""
    startCommand: python app.py
    envVars:
      - key: GOOGLE_APPLICATION_CREDENTIALS
        value: credentials.json
